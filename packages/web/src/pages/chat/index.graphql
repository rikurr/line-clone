subscription MessageAdded($chatId: Int!) {
  message(where: {chat_id: {_eq: $chatId}}, order_by: {created_at: asc}) {
    id
    chat_id
    sender {
      id
      username
      picture
    }
    content
    created_at
  }
}

# mutation MessageBox($chatId: Int!, $content: String!, $sender_id: String!) {
#   insert_message(
#     objects: [{ chat_id: $chatId, content: $content, sender_id: $sender_id }]
#   ) {
#     affected_rows
#   }
# }

# mutation AddMessageBox($chatId: Int!, $content: String!) {
#   insert_message(
#     objects: [{ chat_id: $chatId, content: $content }]
#   ) {
#     affected_rows
#   }
# }